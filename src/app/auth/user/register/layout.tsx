// app/mahasiswa/login/layout.tsx
import { ReactNode } from "react";

export const metadata = {
  title: "Daftar Mahasiswa | E-Pustaka",
};

export default function MahasiswaLoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
