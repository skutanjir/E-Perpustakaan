// app/admin/register/layout.tsx
import { ReactNode } from "react";

export const metadata = {
  title: "Daftar Admin | E-Pustaka",
};

export default function AdminRegisterLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
