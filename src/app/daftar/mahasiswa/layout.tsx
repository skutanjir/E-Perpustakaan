// app/mahasiswa/register/layout.tsx
import { ReactNode } from "react";

export const metadata = {
  title: "Daftar Mahasiswa | E-Pustaka",
};

export default function MahasiswaRegisterLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
