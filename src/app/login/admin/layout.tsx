// app/admin/login/layout.tsx
import { ReactNode } from "react";

export const metadata = {
  title: "Masuk Admin | E-Pustaka",
};

export default function AdminLoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
